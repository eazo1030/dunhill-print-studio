<Window x:Class="Dunhill.PrintStudio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Dunhill.PrintStudio.Views"
        Title="Dunhill Print Studio"
        Icon="/assets/app-icon-1024.png"
        Height="720" Width="1180"
        MinHeight="540" MinWidth="920"
        WindowStartupLocation="CenterScreen"
        Background="#F4F5F7">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top header / printer status bar -->
        <Border Grid.Row="0" Background="#0F172A" Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                    <Image Source="/assets/app-icon-1024.png" Width="28" Height="28"
                           Margin="0,0,12,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <Ellipse Width="10" Height="10" Margin="0,0,8,0"
                             x:Name="StatusDot" Fill="#EF4444"/>
                    <TextBlock Text="Dunhill Print Studio" Foreground="White"
                               FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="PrinterStatusText" Foreground="#94A3B8"
                               FontSize="13" Margin="20,0,0,0" VerticalAlignment="Center"
                               Text="Disconnected"/>
                </StackPanel>
                <TextBlock Grid.Column="1" x:Name="ClockText" Foreground="#94A3B8"
                           FontSize="12" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Tab area -->
        <TabControl Grid.Row="1" Margin="12,12,12,0"
                    Background="Transparent" BorderThickness="0">
            <TabItem Header="Print">
                <views:PrintView/>
            </TabItem>
            <TabItem Header="Designer">
                <views:DesignerView/>
            </TabItem>
            <TabItem Header="Inventory">
                <views:InventoryView/>
            </TabItem>
            <TabItem Header="Queue">
                <views:QueueView/>
            </TabItem>
            <TabItem Header="History">
                <views:HistoryView/>
            </TabItem>
            <TabItem Header="Settings">
                <views:SettingsView/>
            </TabItem>
        </TabControl>

        <!-- Bottom status bar -->
        <Border Grid.Row="2" Background="#E2E8F0" Padding="16,8">
            <TextBlock x:Name="FooterStatus" FontSize="12" Foreground="#475569"
                       Text="Ready"/>
        </Border>
    </Grid>
</Window>
